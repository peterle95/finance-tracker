# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` when present and ADRs under `docs/adr/` relevant to current work. Missing files are normal; create them lazily when domain terms or architectural decisions are resolved.

Use terminology defined in `CONTEXT.md`. Flag conflicts with existing ADRs instead of silently overriding them.

Expected layout:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
